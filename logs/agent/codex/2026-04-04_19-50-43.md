# 作業ログ

- 日時: 2026-04-04 19:50:43
- 対象機能 / 対象ファイル:
  - 本番ドメイン設定
  - `frontend/.env.production`
  - `backend/.env.production`
  - `backend/config/initializers/cors.rb`
  - `backend/config/application.rb`
  - `backend/app/controllers/application_controller.rb`
  - `backend/config/environments/production.rb`
- 変更内容の要約:
  - フロントエンド本番ドメインを `https://lifehub.jongin.blog` に設定
  - API 本番ドメインを `https://api.lifehub.jongin.blog` 前提に揃えて設定
  - フロントの `NEXT_PUBLIC_API_URL` と `NEXTAUTH_URL` を本番ドメインに更新
  - Rails 側の `FRONTEND_ORIGIN` を `https://lifehub.jongin.blog` に更新
  - CORS 設定をカンマ区切り複数 origin 対応に拡張し、将来 staging や追加ドメインを許可しやすくした
  - Rails の URL 生成で `protocol` と公開ポートを扱えるようにし、HTTPS 配下で不要な `:3001` が露出しないよう `RAILS_PUBLIC_PORT` を導入
  - production 環境で `RAILS_HOST` を `config.hosts` に追加し、Host Authorization に備えた
- 実装理由または判断根拠:
  - 現行コードでは `frontend/src/lib/api.ts` が `NEXT_PUBLIC_API_URL` を参照して API ベース URL を構築しているため、フロント公開ドメインに対応する API 公開ドメインを明示する必要があった
  - 現行コードでは Rails 側で `FRONTEND_ORIGIN` をそのまま CORS 許可 origin に使っていたため、本番フロントドメインへ更新した
  - Rails の `default_url_options` は元々 `PORT=3001` を常に含む形だったため、リバースプロキシ配下の本番 URL 生成で `https://api.lifehub.jongin.blog:3001` のような不正確な URL になる可能性があった
  - 公開 URL とアプリ内部待受ポートを分離するため `RAILS_PUBLIC_PORT` を導入し、本番 HTTPS の公開ポート `443` を扱えるようにした
- 実行した確認内容:
  - `ruby -c backend/config/application.rb`
  - `ruby -c backend/app/controllers/application_controller.rb`
  - 変更ファイルの差分確認
  - `.env.production` の内容確認
- 未対応事項や注意点:
  - 今回は `api.lifehub.jongin.blog` を API ドメインとする前提で設定している。実際に別ドメインや同一ドメイン配下のパス運用にする場合は再調整が必要
  - DNS 設定、Nginx / Caddy などのリバースプロキシ設定、TLS 証明書設定はこのリポジトリ外のため未対応
  - Rails production は引き続き `Active Storage :amazon` 前提のため、S3 を使わない場合は `backend/config/environments/production.rb` の見直しが必要
  - プレースホルダーのシークレット値、DB 認証情報、AWS 認証情報は本番実値へ差し替えが必要
- この修正で使うコミットメッセージ:
  - `chore: align production domain and cors settings`
