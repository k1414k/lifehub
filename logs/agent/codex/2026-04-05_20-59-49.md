# 作業ログ

- 日時: 2026-04-05 20:59:49
- 対象機能 / 対象ファイル:
  - 本番認証通信の経路調査
  - `frontend/src/lib/api.ts`
  - `frontend/next.config.ts`
  - `frontend/.env.production`
  - `backend/config/environments/production.rb`
- 変更内容の要約:
  - `NEXT_PUBLIC_API_URL` が `/api` でも絶対 URL でも `frontend` の `baseURL` が二重 `/api` にならないよう正規化を追加
  - `NEXT_PUBLIC_API_URL=/api` のときは `Next.js rewrites` を無効化し、同一オリジン proxy と競合しないようにした
  - production の `frontend` 設定を `/api` 前提へ変更
  - Rails production の `config.hosts` に `FRONTEND_ORIGIN` 由来の host も追加し、同一ホスト配下 proxy 時の Host Authorization を許可
- 実装理由または判断根拠:
  - 配信中の本番 JS バンドルに `baseURL:"".concat("/api","/api/v1")` が含まれており、実際に `/api/api/v1/...` が組み立てられていた
  - 追加調査で `POST https://lifehub.jongin.blog/api/v1/auth/sign_in` は 404、`POST https://lifehub.jongin.blog/api/api/v1/auth/sign_in` は 401 を返し、Nginx の `/api/` proxy が `/api` を 1 段剥がして backend に転送していると判断した
- 実行した確認内容:
  - `npm --prefix "/Users/irie/dev/lifehub/frontend" run build`
  - `ReadLints` で変更ファイルに lint エラーがないことを確認
  - `curl -i https://lifehub.jongin.blog/api/up`
  - `curl -i -X POST https://lifehub.jongin.blog/api/v1/auth/sign_in ...`
  - `curl -i -X POST https://lifehub.jongin.blog/api/api/v1/auth/sign_in ...`
- 未対応事項や注意点:
  - 本番 404 の直接原因はリポジトリ外の Nginx 設定の可能性が高く、このリポジトリ内では修正できていない
  - 想定される修正は `location /api/` の `proxy_pass` から trailing slash を外し、`/api/v1/...` をそのまま Rails に渡すこと
  - Nginx 修正後は frontend が `/api/v1/auth/sign_in` で 404 ではなく Rails の認証応答を返すことを再確認する必要がある
- この修正で使うコミットメッセージ:
  - `fix: normalize production api base path handling`
